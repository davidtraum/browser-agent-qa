Additional Shopware Administration context for AI agents:

- When a task or issue refers to "Settings" without specifying which settings area, check both the Shop settings and the Profile settings.
- Prefer the most relevant area first based on the surrounding issue description, but do not assume "Settings" only means one location unless the task explicitly says so.
